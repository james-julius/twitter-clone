{
  "devDependencies": {
    "chokidar": "^3.5.3"
  },
  "dependencies": {
    "@hotwired/turbo-rails": "^7.1.1",
    "esbuild": "^0.14.39"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds"
  }
}
